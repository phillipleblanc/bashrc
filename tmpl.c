#include <stdlib.h>
#include <stdio.h>

#define DEBUG 1
#define printd if (DEBUG) printf

void run()
{
  int i,j,k,n,w,h;
  scanf("%d", &n);
}

int main()
{
  int count, j;
  scanf("%d", &count);
  for (j=0;j<count;j++)
  {
    printd("Case: %d\n",j+1);
    run();
  }
  
  return 0;
}

